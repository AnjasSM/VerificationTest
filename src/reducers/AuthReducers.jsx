const Initial_State ={ username: '', error: '', loading: false};

export default (state = Initial_State, action) => {
    switch(action.type) {
        case 'USER_REGISTER':
        default :
            return state;
    }
}